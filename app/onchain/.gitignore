# Rust's output directory
target

# Local settings
.soroban
.stellar
.env

# Generated registry (re-generated deterministically from source data)
deployments/contract-registry.json

# Temporary types used during CI verification only
.types-temp/
